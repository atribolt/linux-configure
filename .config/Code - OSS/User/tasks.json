{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clang-format",
            "type": "shell",
            "command": "clang-format",
            "args": ["--style=file", "-i", "**/*.{hpp,cpp}"]
        }
    ]
}